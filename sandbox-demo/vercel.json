{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build:sandbox",
  "outputDirectory": "sandbox-demo/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
